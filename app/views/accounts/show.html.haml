%img{ :src => twitter_profile_image }
- if !@polls.blank?
  %ul
    - for poll in @polls
      %li= link_to "a poll", poll_path(:id => poll[0].to_i)
- else
  You don't seem to have any polls setup just yet