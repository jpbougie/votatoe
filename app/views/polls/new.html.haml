%section#get-started
  #new-tweet
    %h1
      Ask your audience
    = form_for :tweet, :url => polls_path do |f|
      #status-bubble
        = f.text_area :status, :rows => 5, :placeholder => "Do you think...", :autofocus => true
      .speech-arrow
      %button{:type => "submit"}
        %h2 get started!
  
  %a#show-existing{:href => from_existing_polls_path} I have already tweeted my question
